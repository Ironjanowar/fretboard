services:
  fretboard:
    build: .
    ports:
      - "${PORT:-4000}:${PORT:-4000}"
    environment:
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - PHX_HOST=${PHX_HOST:-localhost}
      - PORT=${PORT:-4000}
      - PHX_SERVER=true
    restart: unless-stopped
